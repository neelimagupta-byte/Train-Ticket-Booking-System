# Train-Ticket-Booking-System
A console-based Train Ticket Booking System in C. It supports adding, updating, deleting trains, searching routes, booking tickets with PNR generation, viewing and canceling tickets. Demonstrates file handling, structures and CRUD operations.
